# =============================================================================
# RAG Policy Agent — requirements.txt
#
# Size notes:
#   • sentence-transformers is installed WITHOUT torch to save ~2.5 GB.
#     Inference runs via optimum[onnxruntime] instead (same speed on CPU).
#   • camelot-py is the primary table extractor (best for insurance sublimit
#     tables); pdfplumber is kept as the fallback.
#   • ghostscript + poppler-utils must be present as system packages
#     (installed via apt in the Dockerfile).
# =============================================================================

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── PDF processing ─────────────────────────────────────────────────────────────
PyMuPDF==1.24.14                  # full-text extraction
camelot-py==1.0.0                 # primary table extraction (lattice mode)
opencv-python-headless==4.10.0.84 # required by camelot
ghostscript==0.7                  # required by camelot (Python binding)
# pdfplumber==0.11.4                # fallback table extraction

# ── Embeddings & retrieval ────────────────────────────────────────────────────
# Install sentence-transformers WITHOUT torch to avoid the 2-3 GB torch wheel.
# ONNX runtime provides CPU inference at near-identical speed.
sentence-transformers==3.3.1
optimum[onnxruntime]==1.23.3   # ONNX export + runtime (replaces torch)
faiss-cpu==1.9.0.post1
rank-bm25==0.2.2

# ── LLM ───────────────────────────────────────────────────────────────────────
google-generativeai==0.8.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.32.3
pydantic==2.10.3